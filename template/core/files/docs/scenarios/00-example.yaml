module: example
touches:
  files: []
  tables: []
  ports: []
  routes: []
scenarios:
  - id: EX-S1
    name: Replace this example with a real product scenario
    persona: user
    steps: |
      1. REPLACE_ME with a concrete user action.
    success_signals:
      - REPLACE_ME with an observable outcome.
      - "EXPERIENCE: REPLACE_ME with what the user sees happen."
